# JWT Secret Key (Change this in production!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Base URL for the application
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# SMTP Email Configuration (Optional - for sending password reset emails)
# If not provided, emails will be logged to console instead
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password

# For Gmail: 
# 1. Enable 2-factor authentication
# 2. Generate an "App Password" at https://myaccount.google.com/apppasswords
# 3. Use that app password as SMTP_PASSWORD

# Supabase (Optional - for cloud database instead of file-based)
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
